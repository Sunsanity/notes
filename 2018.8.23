1.实现了并查集的基本方法
  数据用一个数组存储，数据中每个元素代表这个元素的父元素，如果两个元素的父元素相等的话代表两个元素属于连接状态。
查找方法是根据判断传入元素的父元素是否是自己，不是的话继续向上查找，直到找到的元素的父元素还是自己，这时的元素就是要查找元素的分组。
union操作是将找到A元素和B元素的父元素，然后将A元素的父元素设置成B元素的父元素即可，这样两个节点所在组的节点全部合并到一个分组。
