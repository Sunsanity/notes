责任链模式：
  责任链模式就是客户端的一个请求有可能被多个对象处理，多个对象都存有一个后继对象，形成一条责任链，链上的任何一个对象都可以处理请求，每一个对象有两种选择
  1.处理请求 2.将请求传递给下一个对象处理，需要注意的是请求传递到链尾时需要有一个默认的处理逻辑。
  表现：
  1.客户端：存有一个处理器的引用（接口），初始化时将这个引用赋值，这个赋值时可以使用工厂模式赋值，不自己new对象
  2.工厂类：工厂类有一个createHandler方法，工厂类将所有继承了处理器类的对象初始化，然后设置每一个对象的后继对象，形成一条链
  3.处理器类（抽象类）：这个抽象类存有一个自己的引用作为后继者使用，还有一个set方法，在工厂类中调用set方法设置后继者。
    一个抽象方法，主要方法逻辑，具体实现子类实现。
  4.多个具体实现抽象类的子类，每个子类都有一个后继者，并继承父类的方法，内部两种逻辑，一种自己处理，另一种调用自己的后继者，让后继者处理。
  类图见imooc网。
