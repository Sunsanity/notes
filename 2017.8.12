1.写了一个算法
  堆的实现：实现了一个最大二叉堆，特点是用一个数组来实现，父节点都不小于子节点，而且是一个完全堆，也就是最底层的节点是从左到右排列的，不会出现跳节点的现象，每个节点的左子节点的索引值是
	父节点的二倍，右子节点是父节点索引的二倍加一，每个节点的父节点的索引是子节点的二分之一。
	堆中目前实现了shiftup和shiftdown操作，分别是向上移动元素和向下移动元素的方法。利用最大堆的性质我们每次向堆中插入新元素的时候其实已经对数组进行了排序操作，最后遍历执行extratMax的时候
	可以得到一个倒序排列的数组。
  堆的思想中最大的特点是优先队列，相比于普通队列，优先队列并不是简单的先进先出，后进后出，而是每个元素都有一个优先级别，堆能实现每次根据元素的优先级别取出级别高的元素。

2.验证码制作
	验证码制作只需要添加一个jar包，然后按照配置配置就可以了

3.注册后发送邮件激活用户
	了解了邮件服务器和JAVAMAIL发送邮件的过程和原理，可以实现用户注册后向用户发送email的操作。
