1.写了三个算法
  归并排序算法:
	算法的思想是先让整个数组不断的进行等分，最终分成每个元素就是一部分，然后对长度小于15的数组进行排序，递归调用归并算法对两个部分进行排序，
	然后对两个已经有序的部分进行一次归并操作。整个算法的重点就在于归并操作上面。
	优化点1：对于数组长度小于15的采用插入排序算法实现数组的排序，因为长度小的数组有序性会更强一些。
	优化点2：进行归并操作时需要在mid元素>mid+1时进行。否则代表整个数组已经排序完毕，就不进行操作了。
  插入排序算法：
	用一个范围数组重新实现了插入排序算法。
  快速排序算法：
	快速排序算法的思想是以随机一个元素作为标定点，然后和第一个元素交换位置，然后从1开始想最后遍历，每次循环判断当前元素是否小于1的元素，小于的话将其和j位置的元素交换位置，
	这样做的结果是整个循环下来数组被分成了两个部分，小于1元素（左边）和大于1元素（右边），最后传入新的参数递归调用排序方法就可以了。
	难点在于定义j元素的位置，j元素就是第一个元素应该放入的位置。每次随机选取元素作为标定点的目的是避免排序一个几乎有序的数组时使得每次的二分都将数组变的更加不对称。

2.学习了web开发中的监听器原理
	监听器其实就是监听request,session,application这几个域对象的变化状态，如果一旦对象状态发生了变化，就可以调用到我们在监听器中写的逻辑，实现我们的应对策略。

3.学习了web开发中的过滤器原理
4.学习了单例模式
	分为懒汉式和恶汉式，懒汉式可以延迟加载，饿汉式在类被加载时已经被初始化，加载慢，得到对象快，懒汉式加载快，得到对象速度慢。
