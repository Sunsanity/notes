1.写了一个算法
  快速三路排序算法：
	快速三路排序算法相比二路排序算法的区别在于，二路快排遇到等于标定点元素的情况时仍然会交换一下两个元素的位置，而三路快排则不处理这种情况，所以对数组中有大量重复元素的情况排序的效率
	会很高。

2.学习了SSO单点登录
	原理就是服务端通过cookie验证用户身份，正确的话放行主页，cookie写入客户端服务器，错误的话返回登录页面，多个应用用同一套cookie的话就可以达到一处登录处处应用的效果了。正常情况下验证cookie
	的操作应该写在特定的拦截器里面。
