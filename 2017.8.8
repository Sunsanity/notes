1.写了一个算法
  快速二路排序算法：
	二路排序算法相比普通快速排序算法的有点在于可以避免大量重复元素的出现导致了快排后的数组的长度分割越来越不均匀的情况。二路排序的特点在于有两个指针分别从前向后
	和从后向前，如果遇到和标定元素相等的元素直接原地等待，等到另一个指针也碰到相等元素然后两个相等元素也要交换位置，这个过程避免了重复元素始终在一边的情况出现，所以比普通快排相比
	支持了大量重复元素的导致效率低下的情况出现。
